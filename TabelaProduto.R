# vetor
notas <- c(7.5, 8.0, 9.2)
print(mean(notas))
# data frame
vendas <- data.frame(
  produto = c("A", "B" , "C"),
  quantidade = c(10, 15, 5)
)
print(vendas)




produto <- list(
  nome = "Notebook",
  preco = 2500,
  estoque = 10
)
print(nome(produto))
